<!DOCTYPE html>
<html lang="en">
<head>
	<title>Cadastro de Produtos</title>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">	
	<link rel="icon" type="image/png" href="images/icons/favicon.ico"/>
	<link rel="stylesheet" type="text/css" href="vendor/bootstrap/css/bootstrap.min.css">

</head>
<body>
	<div class="container col-md-7">
		<p class="text-right mt-2">
			<span>Usuário: </span>
			<span class="text-primary"><strong><%=nome%></strong></span><br>
			<a href="/sair" class="btn btn-dark mt-2" >Sair</a>
		</p>
		<form action="/cadastrar-produto" method="POST">
			<div class="mt-3">
				<h1>Cadastro de Produtos</h1>
				<a href="/listagem-produtos" class= "btn btn-info mt-3 mb-3">Você pode Ver Todos Produtos cadastrados clicando aqui</a>
			</div>
			<%  if(validacao.length > 0 ) { %>
				<div class="alert alert-danger">
					<strong>Atenção</strong>
					<ul>
						<% for(let i=0; i < validacao.length; i++){ %>
							<li><%=validacao[i].msg%></li>
						<% } %>
					</ul>
				</div>
			<%  } %>
			
			<div class="form-group mt-4">
			  <label for="nomeProduto">Nome do Produto</label>
			  <input type="text" class="form-control" id="nomeProduto" name="nomeProduto" placeholder="Insira um nome" style="font-style:italic">
			</div>
			<div class="form-group">
				<label for="dataValidade">Data de Validade</label>
				<input type="text" class="form-control" id="dataValidade" name="dataValidade" placeholder="Ex: 12/12/2020" style="font-style:italic">
			</div>
			<div class="form-group">
				<label for="localColeta">Local de Coleta</label>
				<input type="text" class="form-control" id="localColeta" name="localColeta" placeholder="Ex: Rua dos Donatários" style="font-style:italic">
			</div>
			<input type="hidden" name="id_user" id="id_user" value="<%=id%>">
			<input type="hidden" name="created_at" id="created_at" value="<%=  new Date().toLocaleString() %>">
			<button type="submit" class="btn btn-success" style="cursor:pointer;">Cadastrar</button>
		  </form>
	</div>

</body>
</html>